# --- shared ---
RPC_URL=https://api.devnet.solana.com
TXLINE_NETWORK=devnet

# --- keeper ---
# KEEPER_KEYPAIR=./keeper/keeper-keypair.json   # auto-generated if absent; fund with devnet SOL
# RECORDINGS_DIR=./recordings

# --- funder ---
# FUNDER_KEYPAIR=./funder/funder-keypair.json   # auto-generated if absent; fund with devnet SOL
FUNDER_PORT=8787

# --- app (app/.env.local) ---
NEXT_PUBLIC_PRIVY_APP_ID=            # from dashboard.privy.io
NEXT_PUBLIC_RPC_URL=https://api.devnet.solana.com
NEXT_PUBLIC_FUNDER_URL=http://localhost:8787
